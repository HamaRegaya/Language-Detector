# CPU-only requirements for Docker serving (no CUDA runtime needed)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
torch --index-url https://download.pytorch.org/whl/cpu
